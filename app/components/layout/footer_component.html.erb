<footer class="mt-auto border-t border-border bg-background px-5 py-6">
  <div class="mx-auto flex max-w-7xl flex-col items-center gap-4 sm:flex-row sm:justify-between">
    <nav>
      <ul class="flex flex-wrap justify-center gap-6">
        <% footer_links.each do |link| %>
          <li>
            <%= link_to link[:label],
                        link[:path],
                        class: "text-text-secondary hover:text-accent transition-colors" %>
          </li>
        <% end %>
      </ul>
    </nav>
    <p class="text-center text-sm text-text-secondary sm:text-right">
      PolyScope — Polymarket risk analysis
    </p>
  </div>
</footer>
